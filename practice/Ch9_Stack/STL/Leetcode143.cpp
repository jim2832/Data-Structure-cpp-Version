#include <iostream>
#include <string>
#include <stack>
using namespace std;

class Solution{
    struct ListNode{
        int val;
        ListNode* next;
    };

    ListNode* head;
    ListNode* tail;
    int len;

    public:
        void reorderList(ListNode* head){

        }
};